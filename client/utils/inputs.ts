/**
 * NEED TO COMPLETE
 * @param input 
 * @returns 
 */
export const formatInputString = (input: String) => {
  return input.trim();
}
 
/**
 * NEED TO COMPLETE 
 * @param input 
 * @returns 
 */
export const formatNumberInput = (input: number) => {
  return input; 
}